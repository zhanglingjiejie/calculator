import { StrToUnion } from "..";
export type Primitive = null | undefined | string | number | boolean | symbol | bigint;
export type VarUnion = StrToUnion<"abcdefghijklmnopqrstuvwxyz_$"> extends infer T ? (T extends string ? (Uppercase<T> | T) : (never)) : (never);
export type Space = " " | "\n" | "\t";
